[
  {
    "key": "ClientId",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "ClientId",
      "description": "The ClientId to connect to the Ysis API",
      "required": true
    }
  },
  {
    "key": "ClientSecret",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "type": "password",
      "label": "ClientSecret",
      "description": "The ClientSecret to connect to the Ysis API",
      "required": true
    }
  },
  {
    "key": "BaseUrl",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "placeholder": "https://your-organization.ysis.nl",
      "label": "BaseUrl",
      "description": "The URL to the Ysis API.",
      "required": true
    }
  },
  {
    "key": "DefaultModule",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "placeholder": "YSIS_CORE",
      "label": "Default Module (code)",
      "description": "The default module code. (YSIS_CORE)",
      "required": true
    }
  },
  {
    "key": "MappingFile",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "MappingFile",
      "description": "The mapping between function and profession",
      "required": true
    }
  },
  {
    "key": "UpdatePersonOnCorrelate",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Update account during correlating",
      "required": false,
      "description": "This will update the account in the target application during correlation"
    }
  },
  {
    "key": "UpdateUsernameOnDelete",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Update username when archiving Ysis account",
      "required": false,
      "description": "Because Ysis accounts cannot be dearchived by HelloID we will update the username to the YSISinitials. This will always result in a unique value so we can reuse the old username for new accounts"
    }
  },
  {
    "key": "IsDebug",
    "type": "checkbox",
    "defaultValue": true,
    "templateOptions": {
      "label": "Toggle debug logging",
      "description": "When toggled, debug logging will be displayed",
      "required": false
    }
  }
]